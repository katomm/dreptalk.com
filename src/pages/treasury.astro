---
export const prerender = false;

// The standalone treasury overview moved into the Budget and Treasury
// discussion category, so this URL just redirects there. Kept as a route
// (rather than deleted) because the mini card and other links still point here.
return Astro.redirect('/c/budget', 301);
---
