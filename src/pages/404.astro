---
import NotFound from '@/components/NotFound.astro';

// Astro serves this for any unmatched route. Same shell as every other
// not-found page, with its own lead: here nothing specific was looked up.
---
<NotFound noun="Page" lead="This page is not on the ballot. The link may have expired, or the page moved." />
